[pytest]
minversion = 6.0
addopts = -q
testpaths = tests
# src            -> host package `camera_follower_bot`
# src/rpi_pico_code -> Pico modules, which import each other by bare name
#                     (`from remote_logger import ...`) as they do on the device
pythonpath = src src/rpi_pico_code
